[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bs-pricer"
version = "0.1.0"
description = "Pricer de Black-Scholes"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "scipy",
    "pytest"
]